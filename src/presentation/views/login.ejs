<%- include partials/top-page %>

    <!-- Our application root element -->
    <div id="app">
      <div class="mx-auto" style="width: 500px; height: 400px;" >
        <div id= "forma" class="text-center">
              <b-jumbotron header="Logeate" style="background-color: #efd199;">
                  <form onsubmit="return false" class="form-signin">
        
                    <h1 class="h3 mb-3 font-weight-normal">Please sign in</h1>
                    <label for="inputEmail" class="sr-only">Email address</label>
                    <input v-model="email" id="inputEmail" class="form-control" placeholder="Usuario" required autofocus>
                    <label for="inputPassword" class="sr-only">Password</label>
                    <input v-model="password" type="password" id="inputPassword" class="form-control" placeholder="Contraseña" required>
                    <div class="checkbox mb-3">
                      <label>
                        <input type="checkbox" value="remember-me"> Remember me
                      </label>
                    </div>
                    <button v-on:click="loguear" class="btn btn-lg btn-primary btn-block" style="background-color: #507dbc;">Sign in</button>
                    <p class="mt-5 mb-3 text-muted">&copy; 2017-2020</p>
                  </form>
                
              </b-jumbotron>
        </div>
      </div>
    </div>

    <!-- Start running your app -->
    <script>
      window.app = new Vue({
        el: '#app',
        data: {
          name: '',
          password: "",
          email: ""
        },
        computed: {
          showAlert() {
            return this.name.length > 4 ? true : false
          }
        },
        methods: {
          async loguear(){
            await axios
                .post('/login', {usuario: this.email, password: this.password})
                .then(respuesta => {
                  if(respuesta.status == 200){
                    console.log("Hola",this.email,this.password);
                  } 
                  else {
                    console.log("esta mal");
                  }
                })
                .catch(error => {  
                  console.log(error);
                }); 
          }
        }
      })
    </script>

    <!-- Footer -->
  </body>
</html>